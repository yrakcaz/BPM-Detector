BPM-Detector
============

Useful tool to extract the BPM of an any-format song file.

Build:
    ./configure [--prefix=<INSTALL DIR>] && make

Install:
    make install

Usage:
    ./BPMDetector <SONG FILE>

- The make check command works with a JSON Array containing arrays with file and bpm.

- The test.sh script is only able to calculate bpm of every songs of a directory, then put results into a log file.
