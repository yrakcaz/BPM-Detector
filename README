musical-feature-extractor
=========================

Useful tool to extract features from an any-format song file.

Build:
    ./configure [--prefix=<INSTALL DIR>] && make

Install:
    make install

Usage:
    ./musical-feature-extractor --help
